describe('Find Transaction', () => {
    it("Tracking payment", ()=>{
        cy.TrackTransaction();
        // cy.LoginUser();
        // cy.wait(2000)
        // cy.get("[href='/parabank/findtrans.htm']").should('contain.text','Find Transactions').click();
        // cy.get('.title').should('exist').and('contain.text','Find Transactions')
        // cy.get("[ng-model='criteria.onDate']").type('12-27-2022')
        // cy.get(':nth-child(9) > .button').should('contain.text', "Find Transactions").click()
        // cy.get("#transactionTable > tbody > tr > td:nth-child(2) > a").as('btn').click()
        // // cy.get('@btn').click();
        // cy.get('.title').should("contain.html", 'Transaction Details')
        // cy.url().should("contain",'parabank/transaction')
    })
    // #rightPanel > div > div > form > div:nth-child(9) > button
    
  
})
